export default {
    // Use Node environment for testing
    testEnvironment: 'node',

    // Automatically clear mock calls and instances between every test
    clearMocks: true,

    // Directory where Jest stores its cached test results
    coverageDirectory: 'coverage',

    verbose: true,
};
